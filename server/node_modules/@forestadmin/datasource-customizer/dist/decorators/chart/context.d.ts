import { Caller, Collection, CompositeId } from '@forestadmin/datasource-toolkit';
import CollectionCustomizationContext from '../../context/collection-context';
import { TCollectionName, TFieldName, TRow, TSchema } from '../../templates';
export default class CollectionChartContext<S extends TSchema = TSchema, N extends TCollectionName<S> = TCollectionName<S>> extends CollectionCustomizationContext<S, N> {
    readonly compositeRecordId: CompositeId;
    get recordId(): string | number;
    constructor(collection: Collection, caller: Caller, recordId: CompositeId);
    getRecord(fields: TFieldName<S, N>[]): Promise<TRow<S, N>>;
}
//# sourceMappingURL=context.d.ts.map