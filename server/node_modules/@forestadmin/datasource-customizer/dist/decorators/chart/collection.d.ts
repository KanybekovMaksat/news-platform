import { Caller, Chart, CollectionSchema, CompositeId } from '@forestadmin/datasource-toolkit';
import { CollectionChartDefinition } from './types';
import CollectionDecorator from '../collection-decorator';
export default class ChartCollectionDecorator extends CollectionDecorator {
    private charts;
    addChart(name: string, definition: CollectionChartDefinition): void;
    renderChart(caller: Caller, name: string, recordId: CompositeId): Promise<Chart>;
    protected refineSchema(subSchema: CollectionSchema): CollectionSchema;
}
//# sourceMappingURL=collection.d.ts.map