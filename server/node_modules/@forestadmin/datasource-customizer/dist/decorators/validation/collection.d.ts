import { Caller, CollectionSchema, ColumnSchema, Filter, RecordData } from '@forestadmin/datasource-toolkit';
import CollectionDecorator from '../collection-decorator';
type ValidationRule = ColumnSchema['validation'][number];
export default class ValidationDecorator extends CollectionDecorator {
    private validation;
    addValidation(name: string, validation: ValidationRule): void;
    create(caller: Caller, data: RecordData[]): Promise<RecordData[]>;
    update(caller: Caller, filter: Filter, patch: RecordData): Promise<void>;
    protected refineSchema(subSchema: CollectionSchema): CollectionSchema;
    private validate;
    /**
     * Deduplicate rules which the frontend understand
     * We ignore other rules as duplications are not an issue within the agent
     */
    private static deduplicate;
    private static min;
    private static max;
}
export {};
//# sourceMappingURL=collection.d.ts.map